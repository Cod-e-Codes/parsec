{
    "name": "test-app",
    "version": "1.0.0",
    "config": {
        "port": 8080,
        "host": "localhost",
        "database": {
            "url": "postgres://localhost:5432",
            "maxConnections": 10
        }
    },
    "features": ["auth", "api", "websocket"]
} 